# Ghostty window title is driven by tmux (see tmux/tmux.conf.symlink:
# set-titles + set-titles-string reading @ghostty_title). wtpr sets that
# option to "repo - #PR"; other sessions fall back to the pane's dir name.
